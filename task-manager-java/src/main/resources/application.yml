spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task_manager?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 136247

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      zhh:
        taskmanager:
          mapper: debug

server:
  port: 9090
  servlet:
    context-path: /start